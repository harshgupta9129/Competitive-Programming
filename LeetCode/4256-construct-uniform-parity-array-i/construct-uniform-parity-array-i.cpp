class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // Suppose All are even or All are Odd Ok
        // But what if mix then we know that difference of odd and even is always odd so we can always create a all odd elements array
        return true;
    }
};