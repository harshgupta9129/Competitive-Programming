#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n,m,a,b;
    cin>>n>>m>>a>>b;
    cout<<1<<" "<<1<<" "<<n<<" "<<m<<endl;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}